// We need a 6*5 grid
  // navigable
  // a matrix?

// Matrix Datastructure for holding the grid information
function MatrixDataStructure (rows, cols) {
  var _gameMatrix;

  // Initialise _gameMatrix with rows and cols
}

// Grid Engine
// Contains additional methods for querying to know the grids whereabouts
function GridEngine() {
  // MatrixDataStructure
}

var test = {
  name: "Anvesh",
  age: 31,
  get getName() {
    return this.name;
  },
  set setName(val) {
    this.name = val;
  }
};

// test.value = 5;
console.log(test);
